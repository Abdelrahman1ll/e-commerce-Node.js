version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: e-commerce-app
    container_name: e-commerce
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    command: npm run dev
